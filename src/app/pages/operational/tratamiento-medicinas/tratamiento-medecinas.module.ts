export interface TreatmentsMedicines {
    description: string;
    periocityDay: number;
    medicinesId: number;
    treatmentId: number;
    medicines: string;  // Esto puede ajustarse según sea necesario (nombre del medicamento)
    treatment: string;  // Esto puede ajustarse según sea necesario (nombre del tratamiento)
  }
  